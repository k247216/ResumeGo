<template><section data-test="local-library" class="library"><div><p>本地资料</p><h1>先整理已有简历，再针对岗位优化</h1><span>你的资料可以独立存在，不需要先创建求职目标。</span></div><button type="button" @click="$emit('create-target')">针对岗位开始优化</button><div class="resume-grid"><article v-for="resume in resumes" :key="resume.id"><strong>{{ resume.title }}</strong><small>当前版本 v{{ resume.currentVersion?.versionNo ?? '-' }}</small><button type="button" @click="$emit('open-resume', resume.id)">继续编辑</button></article></div></section></template>
<script setup lang="ts">import type { Resume } from '../../types/resume'; defineProps<{ resumes: Resume[] }>(); defineEmits<{ (event:'create-target'):void; (event:'open-resume', id:number):void }>()</script>
<style scoped>.library{padding:42px}.library>div>p{color:#168866;font-weight:700}.library h1{margin:6px 0}.library>div>span{color:#74808e}.library>button{margin:22px 0;border:0;border-radius:9px;background:#168866;color:white;padding:10px 14px}.resume-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:14px}.resume-grid article{display:grid;gap:8px;padding:18px;border:1px solid #dfe5e9;border-radius:13px;background:white}.resume-grid small{color:#7c8794}.resume-grid button{justify-self:start;border:0;background:none;color:#168866;padding:0}</style>
