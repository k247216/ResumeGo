# 职达 Career OS V2.0 发布前验证报告

验证日期：2026-08-29（Asia/Shanghai）

这份报告只记录当前发布候选在本地可重复验证的结果。页面 smoke check 使用本地 Vite 预览地址，所有输入数据均来自本机工作区，不包含个人密钥或外部服务凭据。

## 页面级 smoke check

以下路由均成功加载非空页面，浏览器控制台错误数为 0：

| 路由 | 页面 | 结果 |
| --- | --- | --- |
| `/` | Workspace 工作台 | 通过 |
| `/targets` | Career Pipeline 求职计划 | 通过 |
| `/knowledge` | Knowledge Base 知识库 | 通过 |
| `/resumes` | Resume System 简历库 | 通过 |
| `/editor` | 简历编辑台 | 通过 |
| `/evidences` | 能力证据库（兼容入口） | 通过 |
| `/interview?view=home` | 面试主页 | 通过 |
| `/schedule` | 日程 | 通过 |
| `/settings` | 本地设置与 AI 配置 | 通过 |

`/home` 是指向 Workspace 的兼容重定向，不单独渲染页面。

## 自动化验证

### 前端

```text
npm test -- --run
Test Files  79 passed (79)
Tests       441 passed (441)
```

```text
npm run build
✓ built
```

构建存在既有非阻断提示：依赖中的 `INVALID_ANNOTATION`、知识库动态导入未拆分提示，以及主 bundle 超过 500 kB。它们不影响本次构建退出码，但应列入后续性能任务。

### 后端

```text
mvn test
Tests run: 580, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

### 桌面打包

`desktop:prepare` 已通过：后端 JAR、前端生产资源、Electron 主进程和 preload 均成功构建。随后使用网络权限完成 `desktop:pack`，退出码为 0，生成 macOS arm64 应用目录：

```text
output/desktop/mac-arm64/职达 Career OS.app
```

产物包含前端静态资源、`resume-go.jar` 和精简 Java runtime；`Info.plist` 的应用版本为 `2.0.0`。当前构建使用 Electron 默认图标且未签名，这是发布前已知限制，不影响本地启动验证，但正式分发前应补充应用图标、代码签名和公证。

## 功能边界与已知限制

- 未配置真实模型密钥时，简历编辑、知识库、管线、日程和历史浏览保持可用；面试房间会提示先配置 AI。
- 本次自动化测试使用 Mock/隔离的 H2 数据，不代表外部模型服务的可用性或配额。
- 真正的 Windows portable 包需要在 Windows 环境单独构建；macOS 产物也需要在对应系统签名、安装和启动回归。
- 构建警告和安装包签名/公证不属于当前代码回归失败，但会影响正式分发质量。
