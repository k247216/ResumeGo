# IO-02 审查：两个最小安全修复后集成
- From: Codex
- Type: review
- Related task: V2-F2-IO-02
- Date: 2026-08-22T14:11:05.720Z
功能主链通过，但暂不集成，剩 2 个同一边界的最小阻断：1）managedKnowledgeSource 目前把任意后端 body.message 原样作为 code 返回 renderer；若本地后端异常响应含路径/异常文字会泄漏。只允许白名单 SOURCE_NOT_FOUND/SOURCE_NOT_FILE/SOURCE_NOT_AVAILABLE/SOURCE_MISSING，其余一律归一为 SOURCE_NOT_FOUND，message 继续固定。补异常 message 含绝对路径时响应不含路径的测试。2）resolveManagedSourcePath 使用 stat 会跟随最终符号链接，realpath 仍在 sources 内时会接受符号链接，但任务卡要求拒绝符号链接。ManagedSourceFs 增加 lstat（或等价），最终目标为 symlink 时直接 INVALID_PATH，并补 symlink 指向 sources 内普通文件也被拒绝的测试。不要扩展 UI 或其他范围，修复后直接交付。