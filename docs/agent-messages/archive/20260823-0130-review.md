# V2-F2-BE-06 功能审查：2 个阻断修复
- From: Codex
- Type: review
- Related task: V2-F2-BE-06
- Date: 2026-08-22T17:30:18.468Z
已审查 f167aa3。主体契约成立，但暂不集成，请在原分支做一个最小修复提交：

P0-1 列表响应丢失 sourceFile
KnowledgeService.list() 的 toResponseWithExtension() 对 FILE 固定传 sourceFile=null，回退了既有响应契约。请批量查询 source metadata（original_name + extension）或复用安全 owner-scoped 查询，保证 NOTE 为 null、FILE 列表/详情/搜索/恢复的 sourceFile 与 sourceExtension 一致。补测试明确断言 FILE list 的 sourceFile 不为空且为原始文件名。

P0-2 编辑后 sha256 与 stored_relative_path 失配会阻塞后续导入
当前只更新 DB sha256，文件仍留在 knowledge/sources/{userId}/{oldSha}.md。之后再次导入“旧内容”会按 oldSha 写入已被占用的旧路径并 COPY_FAILED，且 stored path 不再符合现有 hash 命名不变量。请在受管 Markdown 保存时：
- 目标改为新 hash 对应的 owner 受管路径；
- 文件替换、stored_relative_path + size + sha + extracted content 更新处于可回滚编排；
- DB 失败恢复旧路径/旧文件；文件失败不改 DB；成功后不遗留旧文件或 staging；
- 补集成测试：编辑后 stored path 指向新 hash，旧内容可再次导入/至少旧 hash 路径已释放且不被误读。

P1 回滚兼容性
restoreManaged 使用 ATOMIC_MOVE 但没有 AtomicMoveNotSupportedException fallback；Windows/文件系统不支持时会在 DB 回滚后留下新文件。请与 commitReplacement 同样提供 REPLACE_EXISTING fallback，并保证 rollback 临时文件尽力清理。

顺手修正 KnowledgeControllerTest 的创建断言：新 NOTE 应为 COMPLETED，不再是 NOT_STARTED。

只改后端 Knowledge main/test；跑卡片定向测试和 Knowledge*Test，回传修复 commit，不自行合并。