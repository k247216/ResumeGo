# FE-02 退回：资料浏览与状态一致性阻断
- From: Codex
- Type: review
- Related task: V2-F2-FE-02
- Date: 2026-08-22T15:04:24.708Z
FE-02 暂不批准集成。Core 已完成代码与 1080×720 实页检查，视觉骨架可保留；请只修以下功能阻断，不重做布局。

P0-1 文件夹/标签浏览是假交互
- 当前 selectFolder/handleTag 只设置 search filter；runSearch 在 q 为空时直接清空结果，而 DocumentList 在 hasSearch=false 时仍显示 documents 全量。
- 结果：点击任意文件夹/标签，标题变化但资料列表不变化。
- Core 明确授权本次最小扩展 backend Knowledge 只读浏览能力：为资料列表提供 categoryId/tagId/includeDescendants 可选过滤（可扩展现有 GET documents，或新增单一 browse endpoint；优先扩展 GET documents）。
- 必须由后端按 current user 和真实关联过滤，父分类包含后代；前端不得 N+1 拉 classification 后本地伪过滤。
- 添加真实端到端契约测试：无 q 点击文件夹/标签，列表只显示对应资料；“全部资料”恢复全量。

P0-2 状态一致性
- 新建 NOTE/导入后自动归入当前文件夹时，setCategory 失败被 catch(() => undefined) 静默吞掉。保留创建/导入成功，但必须显示明确的“资料已创建/导入，归档失败”局部警告并保持真实未归档状态。
- NOTE 保存成功后当前代码用 new Date() 伪造 document.updatedAt；改为回读 getKnowledgeDocument，状态与时间均以后端为准。
- setCategory、删除资料、新建/导入归档成功后刷新 categoryTree，使 direct/descendant counts 立即一致。失败不提前改计数。

P0-3 搜索定位
- 当前选择搜索结果后立即 scrollToLine，但正文尚未加载/DOM 尚未切换，定位经常无效。
- 在目标文档正文加载完成后再执行一次待处理 lineNumber 定位；没有 lineNumber 只打开，不伪造定位。至少加入组件/视图测试证明调用顺序。

P1 必须补齐的诚实交互
- categoryTree 加载/创建/移动/删除错误目前大多没有可见承载；在导航区域显示局部错误和重试，不弹全局通知。
- “Ctrl/Cmd+K”写在 placeholder 但没有实现：实现真正聚焦，或删除快捷键文案。
- 标签创建入口在重构中丢失；恢复一个紧凑入口，继续走真实 createTag。
- 1080×720 保持当前可读布局；补充窄宽时自动先关 inspector、再收列表的行为。收起后把 focus 移到恢复按钮；不要求扩大视觉测试。

视觉修正（不阻断核心逻辑，但本轮顺手）
- 文件类型图标不要使用 emoji，改用项目现有图标库；不要创造 SVG/字符图标。
- 搜索 placeholder 不要声称“搜索标签”，除非后端 q 真支持标签名；标签已有独立筛选。

允许范围增量：frontend 现有 FE-02 文件 + backend/src/main/java/com/resumego/knowledge/** + 对应 Knowledge tests；不得新增 migration、AI/RAG/图谱或全局导航改造。
请在原 FE-02 分支追加最小修复提交，回传 commit、browse API 契约及关键流程验证。