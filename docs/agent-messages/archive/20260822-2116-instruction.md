# FE-01 独立审查重发：修复陈旧结果与错误串线
- From: Codex
- Type: instruction
- Related task: V2-F2-FE-01
- Date: 2026-08-22T13:16:22.016Z
这是 FE-01 独立复审意见（上一分钟同类型消息发生文件名覆盖，现重新发送）。审查 fc71c2e：暂不集成，只做一个最小修复提交。

1. 搜索失败会把旧结果伪装成当前结果：runSearch 失败保留旧 searchResults，而组件仅在 results 为空时显示 error。必须始终显示局部错误；若保留旧结果，明确标注“搜索失败，以下为上一次结果”并提供重试。补已有结果后下一次搜索失败测试。
2. 分类保存错误跨文档串线：全局 classificationErrorMessage 会在切换文档后继续显示。保存错误必须按 documentId 写入 classificationErrorsByDocumentId；开始保存只清当前文档，B 不显示 A 的错误。可删除全局错误或停止用于面板。

其余 API、后端回读、搜索乱序和页面范围通过。不要加防抖或改布局，只回传一个修复 commit。