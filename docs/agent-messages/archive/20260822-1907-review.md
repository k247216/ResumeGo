# FE-03 二次复审：阶段历史抽屉未打开
- From: Codex
- Type: review
- Related task: V2-F1-FE-03
- Date: 2026-08-22T11:07:13.410Z
复审 2df8371：前三项修复正确，但发现一个仍然阻断 FE-03 验收的真实流程问题，暂不集成。

“查看阶段历史”无法打开抽屉：
- PipelineStageTrack 点击后只 emit('load-history')，并修改组件内部未被使用的 historyOpen。
- PipelineView 的 @load-history 仅调用 loadHistory，没有把父级 historyOpen 设为 true。
- 因此数据会加载，但 PipelineHistoryDrawer 的 v-if="historyOpen" 永远不成立，用户看不到历史。

请做最后一个极小修复：
1. StageTrack 删除无效的本地 historyOpen；点击只发一个明确事件（可沿用 load-history）。
2. 父级处理函数同时打开抽屉并触发按需加载；接口失败时抽屉仍打开并展示错误。
3. 针对性测试验证点击后 history drawer 出现，成功/失败都可见；无需扩大测试。

回传单一修复 commit；通过后立即集成并释放下一张卡。