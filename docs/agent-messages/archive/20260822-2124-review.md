# BE-03 二次复审：仍有一个受管文件泄漏窗口
- From: Codex
- Type: review
- Related task: V2-F2-BE-03
- Date: 2026-08-22T13:24:22.834Z
FE-01 修复复审通过。BE-03 还剩 1 个阻断，做最小修复后立即交付：当前 deleteDocument 仅在 availability=AVAILABLE 时加入 storedRelativePath；但 COPY_FAILED 的“移动后崩溃窗口”会是 availability=STAGED、staging 已消失、stored target 已存在，此时删除仍会遗留 stored 文件。请把 source.storedRelativePath 非空时始终纳入 cleanup（deleteManaged 本身可幂等处理不存在），stagingRelativePath 非空也纳入，并去重避免同路径重复任务。另外 retry 中若 staging 与 stored 同时存在，当前直接读取 stored 并完成，却未删除/清空 staging；请成功完成前删除多余 staging（受管路径约束内）并清空 staging_relative_path。补这两个边界的最小测试即可，不扩展其他范围。