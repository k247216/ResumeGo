# 领取 V2-F2-FE-02：完整 Knowledge Library 页面
- From: Codex
- Type: task-card
- Related task: V2-F2-FE-02
- Date: 2026-08-22T14:45:13.888Z
Knowledge 后端依赖现已全部集成。请立即领取完整前端页面任务，必须从精确基线新建独立 worktree，不得沿用 BE-05 工作树。

# V2-F2-FE-02：Knowledge Library 页面

## Identity

- Status: `READY`
- Branch: `codex/v2-f2-fe-02-library-ui`
- Base commit: `03788f3`
- Depends on: integrated F2-IO-02, F2-BE-04, F2-BE-05, F2-FE-01 and approved F2-UX-01

## User result

用户可以在独立 Knowledge Library 页面导入、分类、搜索、定位、重试和彻底删除本地资料，所有状态由真实后端数据驱动。

## Planned scope

- 按批准视觉稿实现层级资料库、可收起资料列表、正文阅读区和可关闭来源检查器。
- 使用已集成 store；补充 BE-03 的 retry/delete client actions。
- import progress、空/加载/失败/重试/无结果/缺失原文件状态。
- 删除影响摘要、明确确认和成功后的列表一致性。
- DesktopShell 的 Knowledge 一级入口已存在，不重做全局工具栏。
- 视觉与交互必须遵循 `docs/superpowers/specs/2026-08-22-v2-knowledge-library-interaction-design.md` 和 `docs/design/v2-knowledge-library-target.png`：层级资料库与标签在左、可收起资料列表居中、正文为主阅读区、来源/属性/关系在可关闭右检查器。
- NOTE 正文允许用户手动编辑并明确保存；FILE 正文只读。搜索命中正文时打开对应文档并定位/突出命中上下文，不制造不存在的内容。
- 1080×720 时至少保留全局工具栏、文档主区和必要操作；优先自动收起检查器，其次收起资料列表，不允许横向溢出遮住主操作。

## Scope out

不做 RAG、AI 总结、知识训练、网页抓取、拖入任意目录监控、Pipeline 私有知识、虚构资产关系、未支持文件类型或全面导航重做。

## File ownership to freeze

router、DesktopShell 的单一导航改动、Knowledge view、`components/knowledge/**`、BE-03 client/store 增量和对应测试。禁止修改 backend、其他 view 或全局视觉系统。

## Required tests

导入流程、层级分类与标签、列表/检查器收起、搜索与正文定位、NOTE 保存、打开原文失败、重试、删除确认与失败恢复、刷新持久化、1080×720、深浅主题与键盘。运行 Knowledge 定向测试、`npm run build`、`npm run build:electron`；全量只在交付前一次。

## Direct rejection

- 写死“技术知识”“JVM”等示例树、虚构文档关系或用静态卡片伪装后端数据。
- 把正文、关系或来源信息再次堆进同一主画布；检查器必须可关闭，资料列表必须可收起。
- 顺带重做全局导航、加入 AI 聊天、知识图谱、RAG、自动保存或任意目录监控。
- 删除失败后提前从列表移除文档，或保存失败后把未落库正文显示成已保存。


补充执行要求：
- 精确 Base: 03788f3
- 视觉参考必须读取：docs/design/v2-knowledge-library-target.png
- 交互契约必须读取：docs/superpowers/specs/2026-08-22-v2-knowledge-library-interaction-design.md
- 所有文件夹、标签、资料、正文、状态和关系均来自现有真实 API；禁止默认示例和静态伪数据。
- 优先完成可演示闭环：导入/新建 NOTE → 分类/搜索 → 打开正文 → NOTE 明确保存 → FILE 打开/定位 → 重试/删除。
- 不修改 backend，不扩展 RAG/AI/图谱/全局视觉。
- 完成后回传提交 hash、真实用户流程、1080×720/深浅主题处理和必要验证；不得自行合并。