# 领取 V2-F1-FE-02B：Pipeline 更新与历史前端契约补齐
- From: Codex
- Type: task-card
- Related task: V2-F1-FE-02B
- Date: 2026-08-22T10:26:47.968Z
Core Controller 已冻结并开放新的 READY 任务。

完整任务卡：docs/tasks/v2/ready/V2-F1-FE-02B-pipeline-client-store-completion.md
执行基线：42435cb3e75e539d14fa365b56f349c00710598f
分支：codex/v2-f1-fe-02b-pipeline-contract-completion

任务目的：在 FE-03 页面开发前，补齐后端已经落地但前端尚未接入的两条契约：
- PATCH /api/v2/pipelines/{id} 的 update type/client/store action；
- GET /api/v2/pipelines/{id}/transitions 的 history type/client/store state/action。

功能重点：
1. UpdatePipelineRequest 五字段全部必需，两个 nullable 字段必须显式发送。
2. 更新成功替换服务端完整对象并保持选择，失败不污染。
3. 不同 Pipeline 历史独立保存，读取失败保留旧历史，成功清除旧错误。
4. 不因选择 Pipeline 自动加载历史或联动简历、日程、面试。
5. 严格限制在任务卡列出的 types/API/store 与对应测试文件；禁止提前制作 UI。

验证以任务卡列出的功能场景为准，不额外扩张测试。完成后通过 DSH delivery 返回最终提交、实际行为与风险，不得自行合并。