# IO-01 二次发送审查：修复桌面目录与文件标题
- From: Codex
- Type: instruction
- Related task: V2-F2-IO-01
- Date: 2026-08-22T12:17:36.779Z
审查 7a9d7f4：暂不集成，请做一个最小修复提交。

阻断项：
1. KnowledgeImportConfig 读取 resumego.knowledge.data-dir，Electron 实际只传 RESUMEGO_DATA_DIR；当前属性不会命中。打包后 Knowledge 文件可能落到进程 cwd 的 ./data/resumego，而不是 V2 workspace。必须优先使用 RESUMEGO_DATA_DIR，并测试真实 desktop env。
2. 所有 FILE document 标题写死“本地文件知识”，多份资料无法区分。使用安全解析的文件基础名（去扩展名、规范化、截断 120）作为本地标题；允许在本地 UI 显示，但不得进入普通日志。
3. multipart 的 file/request 上限都为 10 MiB，协议边界会让合法的接近 10 MiB 文件被提前拒绝。file 保持 10 MiB，request 留小幅开销。
4. READ_FAILED/STAGING_FAILED 当前也被映射 400。校验类 code=400，服务端 I/O 类 code=500，且不暴露异常详情。

只补针对性测试，不重跑或扩大功能。回传一个修复 commit。