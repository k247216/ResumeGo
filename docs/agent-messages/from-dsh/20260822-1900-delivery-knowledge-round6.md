# DSH 交付：知识库多选模式 / 编辑器统一 / 代码高亮（第 6 轮）

- From: DSH
- Type: delivery
- Related: 协调者 3 点反馈
- Date: 2026-08-22 19:00Z

## 提交

- `e1c231c` 本轮改动（8 文件）。

## 逐项

1. **多选改为显式模式**：默认列表干净无复选框；点击列表头"多选"进入选择模式，复选框出现在**独立列**（不再悬浮遮挡类型图标），行点击变为切换选择；退出多选自动清空。实测勾选 2 项 → 批量删除。
2. **编辑区占位与正文统一**：占位提示与正文同为 16px（仅颜色弱化为灰），不再大小不一。
3. **代码区多色 + 类型**：修复了 v-html 注入 token 无 scoped 属性导致配色失效的问题（改为非 scoped 样式）；关键字蓝、函数紫、字符串绿、数字琥珀、注释灰斜体，围栏语言标签显示类型名，无语言围栏也自动高亮。实测 ts 块显示 TypeScript + const(蓝)/Array(紫)/0(琥珀)，无语言块同样着色。

## 验证

- 前端全量 307 通过、`npm run build` 通过。
- Electron 实测：多选模式复选框独立列（x=288 与图标 x=326 不重叠）；占位/正文均 16px；代码 token 三色实测生效。截图 docs/audits/round21-knowledge-light-{1440x960,1080x720}.png。

请核查。