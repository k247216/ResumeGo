# DSH 交付：BE-03 二次复审修复完成（受管文件泄漏窗口）

- From: DSH
- Type: delivery
- Related task: V2-F2-BE-03
- Date: 2026-08-22 13:30Z

修复 commit：
40c3ea474c24d276cca0fed17ebd3a8da5ae8f82

（分支 codex/v2-f2-be-03-recovery-delete，叠加在 e21d12b 之上，未自行合并）

## 阻断修复

1. **deleteDocument 受管路径收集**：`source.storedRelativePath()` 非空时**始终**纳入 cleanup（不再依赖 availability=AVAILABLE，覆盖“availability=STAGED、staging 已消失、stored target 已存在”的移动后崩溃窗口）；`stagingRelativePath` 非空也纳入；两者相同则去重。`deleteManaged` 对不存在的文件幂等（deleteIfExists），不会误报失败。
2. **retry 多余 staging 清理**：staging 与 stored 并存时，读取 stored 完成前删除多余 staging 副本（受管路径约束内）并清空 `staging_relative_path`，不再泄漏。

## 测试

- 新增：`deletingCopyFailedCrashWindowRemovesStoredTarget`（STAGED + staging 缺失 + stored 存在 → 删除后 stored 文件消失，2 个真实 cleanup job 均 COMPLETED 且路径清空）；`retryRemovesExtraStagingWhenStoredTargetAlsoExists`（并存 → COMPLETED 后 staging 消失 + staging_relative_path 清空）。
- 知识模块 129 全过；交付前一次后端全量 **466 通过，0 失败**；git diff --check clean；diff-check 仅限 Allowed files。

请审查后集成。
