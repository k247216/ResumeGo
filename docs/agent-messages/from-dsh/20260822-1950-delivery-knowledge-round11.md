# DSH 交付：外部打开图标按钮 + 不可预览类型入口 + 白色背景统一（第 11 轮）

- From: DSH
- Type: delivery
- Related: 协调者 2 点反馈
- Date: 2026-08-22 19:50Z

## 提交

- `9fb1eac` 本轮改动（3 文件）。

## 1. "在其他软件打开"改为图标按钮

- 媒体查看器工具条：文字按钮 → 紧凑图标按钮（external-link 图标），悬停 title 提示"在其他软件打开"，不再占文字空间。

## 2. 不可预览类型也有打开入口

- 无法在应用内预览的类型（xlsx 等）的通知区新增"在其他软件打开"按钮（图标+文字），桌面端任意类型都可调起系统默认应用；浏览器模式诚实提示"该功能仅桌面端可用"。
- 通知区背景显式改为白色，杜绝灰色渗入（此前网页端看到的灰是通知区未设背景 + 旧构建缓存）。

## 关于网页端无法外部打开

- 桌面端已解决（IPC 调系统默认应用，实测打开成功）；网页端受浏览器沙箱限制无法调起本机应用，按钮会诚实返回"该功能仅桌面端可用"，不伪装。这是 Web 平台固有限制，非缺陷。

## 验证

- 前端全量 307 通过、`npm run build` 通过。
- Electron 实测：PDF 工具条为图标按钮（svg + title、无文字）、视口纯白；xlsx 通知区有打开按钮、背景纯白。
- 截图 docs/audits/round26-knowledge-light-{1440x960,1080x720}.png。

请核查。