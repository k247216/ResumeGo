# DSH 交付：BE-03 审查修复完成（彻底清理 + 崩溃恢复）

- From: DSH
- Type: delivery
- Related task: V2-F2-BE-03
- Date: 2026-08-22 13:22Z

修复 commit：
e21d12bb68078cd789f9c6c352f1710e8d4a51f6

（分支 codex/v2-f2-be-03-recovery-delete，叠加在 2f52002 之上，未自行合并）

## 阻断 1：COPY_FAILED 删除不再遗留 staging 副本

- deleteDocument 为每个真实受管路径创建 cleanup job：AVAILABLE 的 stored path 与**非空 stagingRelativePath 分别各建一个 PENDING job**；无任何路径才建 null-path job（直接 COMPLETED）。
- afterCommit 逐个 job 删除对应受管文件后标记 COMPLETED，任一失败该 job FAILED（FILE_DELETE_FAILED）。
- 新增集成测试：COPY_FAILED（STAGED + staging 路径）文档删除后 staging 副本确实消失，且 cleanup job 为真实 COMPLETED 而非 null 伪造。

## 阻断 2：彻底清理不保留个人数据

- V9/V29 未发布，直接移除 `knowledge_cleanup_jobs.document_title` 列；KnowledgeCleanupJob model、cleanupMapper、insertCleanupJob 同步移除 title 参数。
- job COMPLETED 时 `completeCleanupJob` 清空 `source_relative_path`（+ error_code NULL、finished_at）；FAILED/PENDING 保留路径用于恢复。
- 集成测试断言 COMPLETED 后路径为空。

## 阻断 3：重试进程崩溃不再永久卡 RUNNING

- 启动恢复新增 `recoverStuckImportJobs`：把 document 仍 FAILED 的 RUNNING import job 条件重置回 FAILED（保留原 errorCode），随后可再次重试；StartupRunner 先恢复 import job 再恢复 cleanup job。
- retry 的 COPY_FAILED 分支：staging 缺失但**确定性的 stored target 已存在**（移动后崩溃/事务失败窗口）→ 按“文件已落位、数据库尚未完成”跳过移动直接提取，完成原 job；两者都缺失才 409 STAGING_MISSING。仍然不接受任意路径。
- 新增测试：stuck RUNNING（document FAILED）启动恢复 → 重置 FAILED 保留 EXTRACTION_FAILED → 再次 retry COMPLETED；staging 消失但 stored 已落位 → retry 提取成功。

## 测试

- 知识模块 127 全过（+3 集成测试）；交付前一次后端全量 **464 通过，0 失败**；git diff --check clean；diff-check 仅限 Allowed files。

请审查后集成。
