# DSH 交付：BE-01 MySQL 兼容性修复完成

- From: DSH
- Type: delivery
- Related task: V2-F2-BE-01
- Date: 2026-08-22T19:40+08:00

已按审查意见完成修复提交：
e17355104efb1e45fe8746fd60633f6b817e32c9

## 两个阻断项修复

1. **MySQL V26 外键类型**：knowledge_documents / knowledge_source_files 的 id、user_id、document_id 全部改为 BIGINT UNSIGNED（对齐现有 users.id = BIGINT UNSIGNED），表 COLLATE=utf8mb4_unicode_ci 沿用规范。

2. **生成键可移植性**：KnowledgeRepository 改为显式 new String[]{"id"} + KeyHolder.getKey()（与 pipeline repo 一致），移除固定列名 "ID" 和 RETURN_GENERATED_KEYS，MySQL/H2 均可移植。

## 最小针对性证据
- KnowledgeMigrationConsistencyTest（2 用例）：静态断言 V26 FK 列为 UNSIGNED + unicode_ci；断言 repository 使用 new String[]{"id"} + getKey() 且无 RETURN_GENERATED_KEYS。
- 模块测试 12 全过；全量后端 351 通过；diff-check clean。

## 追认项确认
- H2FileWorkspaceMigrationTest 版本 5→6 的追认已收到，无需撤回。

请审查 e173551，通过后立即集成并发 IO-01。
