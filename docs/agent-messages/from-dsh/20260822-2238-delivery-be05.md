# DSH 交付：V2-F2-BE-05 本地笔记正文保存完成

- From: DSH
- Type: delivery
- Related task: V2-F2-BE-05
- Date: 2026-08-22 14:38Z

最终 commit：
fe46d5301ff5f3d6721b5a6026626ecd3a785bfa

（分支 codex/v2-f2-be-05-note-content，Base 5d26fa1，未自行合并）

## 端点与 byte 限制

- `PUT /api/v2/knowledge/documents/{id}/content` body `{content: string}`：只允许当前用户 `sourceType=NOTE`；content 原样保存 UTF-8（**不 trim、不 Markdown 重写**），允许空字符串，最大 **1 MiB UTF-8 bytes**（`content.getBytes(UTF_8).length` 校验）。
- 响应返回现有 `KnowledgeContentResponse(documentId, content)`。

## 事务边界

- `repository.saveNoteContent` @Transactional：DELETE + INSERT upsert 现有 `knowledge_extracted_contents` → NOTE `processingStatus=COMPLETED` → 更新 document `updatedAt`（updateDocumentStatus 已带 CURRENT_TIMESTAMP）。
- FILE 文档 → 409（`仅 NOTE 文档支持保存正文`）；缺失/跨用户 → 404；超限 → 400。**失败不改变旧正文或更新时间**（事务回滚，集成测试断言超限后旧正文与 updated_at 均不变）。

## 搜索一致性

- 正文写入 `knowledge_extracted_contents` 后，现有搜索 SQL 立即读取新正文（无缓存）；集成测试：保存后 `search("深度强化学习")` 立即命中 CONTENT。

## 隐私

- 不记录正文/标题/差异/请求体；service 无任何正文日志（ListAppender 测试断言日志不含正文）。

## 必要测试

- 新增：KnowledgeServiceTest +4（首次/覆盖、空字符串、1 MiB 边界（恰等允许/超限拒绝）、FILE/缺失拒绝）、KnowledgeRepositoryTest +1（upsert + COMPLETED + updatedAt）、KnowledgeControllerTest +3（PUT 200 / FILE 409 / 超限 400）、KnowledgeNoteContentIntegrationTest +6（首次+覆盖、空/Markdown 中文原样、超限回滚保留旧正文与时间戳、FILE/跨用户拒绝、搜索即时命中、日志无正文）。
- `mvn -Dtest='Knowledge*Test' test`：**166 全过**；交付前一次后端全量 **505 通过，0 失败**；`git diff --check` clean；diff-check 仅限 Allowed files（knowledge/** 与 Knowledge tests）。

请审查后集成。
