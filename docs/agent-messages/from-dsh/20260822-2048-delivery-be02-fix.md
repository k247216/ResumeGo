# DSH 交付：BE-02 审查修复完成（文档关联读取 + 可移植 LIKE 转义）

- From: DSH
- Type: delivery
- Related task: V2-F2-BE-02
- Date: 2026-08-22 12:48Z

修复 commit：
a2164aa056f9c1826c26c6b7e4355feeb33d5724

（分支 codex/v2-f2-be-02-classification-search，叠加在 c467e37 之上，未自行合并）

## 阻断 1：文档关联读取

新增 `GET /api/v2/knowledge/documents/{documentId}/classification`：
- 响应 `{ category: KnowledgeCategoryResponse|null, tags: KnowledgeTagResponse[] }`；document 不属于当前用户一律 404。
- category 与 tags 均按 document_id + user_id 隔离（JOIN 关联表时带 user_id）。
- tags 冻结排序 `name ASC, id ASC` 并有测试；空关联诚实返回 `category=null, tags=[]`。
- 未改动 KnowledgeDocumentResponse，不影响已集成前端契约。

## 阻断 2：可移植 LIKE 转义

- 三处搜索 SQL 全部改用 `ESCAPE '!'`（单字符转义，MySQL 反斜杠字符串语义下安全）。
- `escapeLike`：先 `!`→`!!`，再 `%`→`!%`，`_`→`!_`；反斜杠作为普通字面字符，不再充当 escape char。
- 测试覆盖：`%`、`_`、`!`（单独搜索 + 转义符自身字面）、反斜杠的字面搜索，全部走真实 H2 查询（KnowledgeSearchIntegrationTest）；单元断言生成 pattern。

## 测试

- 新增/更新：ServiceTest +3（空关联/有分类标签/外部 404）、RepositoryTest +1（隔离+排序）、IntegrationTest +2（通配符+转义符字面、classification 读取）、ControllerTest +2（200/404）。
- 知识模块 97 全过；交付前一次后端全量 **434 通过，0 失败**；git diff --check clean；diff-check 仅限 Allowed files。

请审查后集成。
