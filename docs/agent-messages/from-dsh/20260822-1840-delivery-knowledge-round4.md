# DSH 交付：知识库文件浏览 / Markdown 渲染 / 交互细节（第 4 轮）

- From: DSH
- Type: delivery
- Related: 协调者 8 点反馈
- Date: 2026-08-22 18:40Z

## 提交

- `b686052` 本轮改动（17 文件）。

## 逐项

1. **全局工具栏提示不再被遮挡**：`.desktop-rail` 提升到 z-index 50（整个工具栏含 tooltip 置于内容之上、对话框之下），实测可见。
2. **文件夹重命名改为行内编辑**：选中文件夹后点工具栏"重命名"，名称直接在树中变为可编辑输入框（自动聚焦、Enter 提交、Esc 取消），不再弹窗；新建子文件夹仍走创建对话框。
3. **导入的文件可以浏览了**：新增后端 `GET /documents/{id}/source`（用户隔离、路径越界/符号链接拒绝、无 token 401）；前端 PDF 用内嵌 iframe、图片直接内联展示，其他类型保留诚实提示。实测 png 内联预览、pdf iframe 预览、跨用户/缺失 404。
4. **标签搜索不再重叠**：按标签浏览时行内只显示所属文件夹路径，不再重复显示当前标签（范围标签已在列表头）。
5. **编辑区占位提示文字缩小**至 14px 弱化，与正文 16px 区分。
6. **Markdown 格式工具栏**（我的建议并已实现）：编辑模式顶部加 加粗/斜体/标题/列表/引用/行内代码/代码块 按钮，光标处包裹或插入语法，保持焦点与选区。
7. **资料库树与标签字体统一为 14px**。
8. **Markdown 渲染能力大幅增强**：支持 H1-H4、加粗/斜体/行内代码/安全链接、表格、引用、分隔线、有序/无序列表、围栏代码块（HTML 先转义杜绝 XSS）；导入 md 的代码区/表格/分割线均可正常浏览。

## 验证

- 前端全量 304 通过、`npm run build` 通过；后端全量 532 通过。
- Electron 实测：tooltip z=50 可见；md 文档表格(2 列头)/代码块/加粗/引用/hr/有序列表全部渲染；编辑工具栏 7 按钮；png 内联预览（blob URL）、pdf iframe 预览；行内重命名输入出现并自动聚焦；标签过滤下行内仅显示文件夹路径。截图 docs/audits/round19-knowledge-light-{1440x960,1080x720}.png。

请核查。