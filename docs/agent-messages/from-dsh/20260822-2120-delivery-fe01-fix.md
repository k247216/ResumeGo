# DSH 交付：FE-01 审查修复完成（陈旧结果标注 + 保存错误按文档隔离）

- From: DSH
- Type: delivery
- Related task: V2-F2-FE-01
- Date: 2026-08-22 13:20Z

修复 commit：
ffd5735fd89be7f5c959857a589db2fdc8ab0f14

（分支 codex/v2-f2-fe-01-knowledge-client-store，叠加在 fc71c2e 之上，未自行合并）

## 阻断 1：搜索失败不再伪装旧结果

- runSearch 失败仍保留旧结果（供参考），但 KnowledgeSearchResults 现在**始终**显示局部错误横幅：无结果时全屏错误 + 重试；有旧结果时顶部横幅「搜索失败：{msg}，以下为上一次结果」+ 重试，结果列表保留但明确标注非当前结果。
- 新增测试：已有结果后下一次搜索失败 → store.searchErrorMessage 设置且旧结果保留（标注由组件承担）；组件横幅断言「以下为上一次结果」。

## 阻断 2：分类保存错误按文档隔离

- 移除全局 classificationErrorMessage；setCategory/toggleTag 开始保存时只清当前文档（delete classificationErrorsByDocumentId[documentId]），失败写入该文档的 classificationErrorsByDocumentId[documentId]。
- 切换文档后不再串线显示他文档的错误；view 面板 error prop 只用所选文档的错误。
- 新增测试：文档 1 保存失败 → 仅文档 1 有错误；文档 2 保存成功后文档 1 错误保留、文档 2 无错误。

## 测试

- 针对性 78 全过（+3）；交付前一次前端全量 **268 通过（54 文件）**；npm run build 通过；diff-check 仅限 Allowed files。

请审查后集成。
